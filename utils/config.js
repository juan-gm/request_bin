// TODO
// Use dotenv npm package if necessary for the 
// I don't know if it's worth it or not
// Also maybe use a .env file for the config

const PORT = 3000
//const MONGODB_URI = process.env.MONGODB_URI

module.exports = {
  //MONGODB_URI,
  PORT
}